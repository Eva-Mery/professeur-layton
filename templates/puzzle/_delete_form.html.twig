<form method="post" action="{{ path('puzzle_delete', {'id': puzzle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ puzzle.id) }}">
    <button class="btn">Delete</button>
</form>
