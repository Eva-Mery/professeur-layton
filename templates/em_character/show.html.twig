{% extends 'backoffice.html.twig' %}

{% block title %}Visualiser{% endblock %}

{% block body %}
    <h1>Visualiser un personnage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ character.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ character.name }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ character.picture|raw }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ character.description|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('e_m_character_index') }}">Retour à la liste</a>

    <a href="{{ path('e_m_character_edit', {'id': character.id}) }}">Modifier</a>

    {{ include('em_character/_delete_form.html.twig') }}
{% endblock %}
